using UnityEngine;

public class Player : MonoBehaviour
{
    public PlayerParams baseParams;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void GetStrongestUpgradePath()
    {
        // compare the three primary stats
        // the one with the largest difference from its base value is the one
        // that the player will transform into
    }
}
